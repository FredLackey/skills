interface:
  display_name: "Easy Reader Terminal"
  short_description: "Install and activate the Easy Reader visual setup"
  default_prompt: "Use $easy-reader-terminal to install, configure, and activate Easy Reader on my machine now."
